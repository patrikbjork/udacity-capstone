apiVersion: v1
kind: Service
metadata:
  name: reverseproxy
  labels:
    app: reverseproxy
spec:
  type: LoadBalancer
  externalName: reverseproxy
  ports:
    - protocol: TCP
      port: 4200
      targetPort: 8080
  selector:
   app: reverseproxy

